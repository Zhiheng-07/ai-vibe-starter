{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && npx --no-install prettier --write --ignore-unknown \"$f\"; } 2>/dev/null || true",
            "statusMessage": "prettier 格式化刚编辑的文件"
          }
        ]
      }
    ]
  }
}
